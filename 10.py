class Solution:
    # @return a boolean
    def isMatch(self, s, p):
        while '**' in p:
            p=p.replace('**','*')
        
        
def match(s,p):
    if s[]
Solution().isMatch('','.***.')
